export const findOdd = (xs: number[]): number => {
  for (let nums = 0; 0 < num; nums++) {
    if (xs.count(nums) > 0 && xs.count(nums) !== % 0) {
      return xs.count(nums)
    } else {
  
      return 0;
    };
  }
}